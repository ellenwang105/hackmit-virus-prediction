.venv/
__pycache__/
*.pyc

# regenerated by scripts/01, 02 and 04 in well under a minute
data/*.parquet

# intermediate prediction dumps; the shared CSV exports below are tracked
results/*.parquet
!results/*.csv
!results/*.md

# structures are re-fetchable with download_structures.py
# (currently still tracked — see README before untracking)
